// object to attach app logic to so it is separate from the angular boilerplate
// todo - consider replacing with commonjs / browserify 
var formatter = {
    controllers: {},
    directives: {},
    services: {}
};
