Build process for minification

Prevent word-wrapping

Implement own Parsing to 
    supply parser errors
    use a sensible node tree (instead of DOM for XML)

Implement own syntax highlighting

Separate into individual XML and JSON parsers

Show multi-line text in XML nodes on multiple lines (as an option)

Order XML elements
Order XML attributes
Order XML attributes on their own lines

Stop using Bootstrap/Angular?
