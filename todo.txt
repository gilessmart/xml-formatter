Build process
(gives me minification and a development server)
    gulp-connect
    gulp-inject 
    gulp-concat
    gulp-clean-css
    gulp-uglify

Watch / Live reload?

Prevent word-wrapping

Implement own syntax highlighting

Allow user to choose XML/JSON
This would allow us to echo XML parser errors back to the user, and gets over the issue where we can't parse '<parsererror />'
Investigate whether JQuery / another XML parser would help 

Stop using Bootstrap/Angular?
